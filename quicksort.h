#include <stdlib.h>
#include <stdio.h>
void my_quicksort (int* list, int n);
void quicksort_body (int* list, int left, int right);
int partition(int * list, int l, int r);
int ninther (int *a, int l, int r);
int median3(int *v, int a, int b, int c);
